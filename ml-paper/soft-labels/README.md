this folder is suppose to implement the sofl label method in audio scene classification. this can be done in both ridge regression and LDA and for both with and without sensor.

The first attempt on comparing LDA method for with and without sensor did not improve the results (in the case of raw data even decreased).