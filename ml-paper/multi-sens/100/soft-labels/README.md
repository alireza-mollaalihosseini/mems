this folder investigates various soft label model for ridge regression, random forrest, and one MLP model.

the ridge regression was the best model with highest test accuracy. then the best model of soft label model called ridge.py.