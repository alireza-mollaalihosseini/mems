in this folder the features were investigated by Gini model (a specified random forrest model). the model gives highest validation (87.2).

also by picking the top important features (based on the scores from gini), and using ridge regression, there is a maximum with highest accuracy (validation of 86.63 with top 1600 featurtes). intrestingly with having more features, this value reduces.